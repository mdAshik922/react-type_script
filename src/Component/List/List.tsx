import React from 'react';

const List = ({items, onClick}  {
    items: string[],
    onClick: () => void;
}) => {
    return (
        <div>
            List
        </div>
    );
};

export default List;