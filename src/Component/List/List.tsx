import React from 'react';

const List = ({items, onClick }:{
        items: string[];
        onClick: (item: string) => void;
    }) => {
    return (
        <div>
            List
        </div>
    );
};

export default List;